svg_icons:
  default: "university"
  type: "list"
  list_type: "compact"

toggle_position:
  default: "bottom"
  type: enum
  choices:
    - "top"
    - "bottom"

toggle_mode:
  default: "single"
  type: enum
  choices:
    - "single"
    - "double"
